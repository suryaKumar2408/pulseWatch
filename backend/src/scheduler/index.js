'use strict';

const {
  DEFAULT_TICK_INTERVAL_MS,
  isMonitorDue,
  MonitorScheduler,
  getMonitorScheduler,
} = require('./monitorScheduler');

module.exports = {
  DEFAULT_TICK_INTERVAL_MS,
  isMonitorDue,
  MonitorScheduler,
  getMonitorScheduler,
};
